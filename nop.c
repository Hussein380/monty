#include "monty.h"
/**
 * no - does nothing
 * @stack: pointer to the stack
 * @line_number: line number where the nop fucntion is called
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
